# This local request looks up the details associated with the 'goliath-faucet
# smart contract. It will return the contents of the smart contract, if it has
# been deployed to the Chainweb node.
#
# To execute this request:
# faucet-request --local faucet-contract-details
#
# If you would like to deploy the faucet smart contract, please see the
# `request/send/deploy-faucet-contract.yaml` request file.

code: (describe-module "free.goliath-faucet")
publicMeta:
  chainId: "0"
  gasLimit: 600
